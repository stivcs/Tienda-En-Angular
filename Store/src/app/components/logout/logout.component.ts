import { Component } from '@angular/core';// se importa el componente

@Component({
  selector: 'app-logout',// se define el selector
  templateUrl: './logout.component.html',// se define la ruta del archivo html
  styleUrls: ['./logout.component.css']// se define la ruta del archivo css
})
export class LogoutComponent {// se define la clase

}
