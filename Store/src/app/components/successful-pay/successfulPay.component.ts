import { Component } from '@angular/core';// import component

@Component({// decorator
  selector: 'app-successful-pay',// se define el selector
  templateUrl: './successfulPay.component.html',// se define la ruta del archivo html
  styleUrls: ['./successfulPay.component.css']// se define la ruta del archivo css
})
export class SuccessfulPayComponent {// se define la clase

}
