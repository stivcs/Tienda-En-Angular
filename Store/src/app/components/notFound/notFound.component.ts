import { Component } from '@angular/core';// import component

@Component({// aqui se importa el component
  selector: 'app-not-found',// aqui se importa el selector
  templateUrl: './notFound.component.html',// aqui se importa el html
  styleUrls: ['./notFound.component.css']// aqui se importan los estilos
})
export class NotFoundComponent { // aqui se exporta el component
}
